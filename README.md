# Make files #

import one of the following in the repo root based on your system environment, they all import makefile-common but override defaults to make setup and compatibility easier

e.g.
``import ~/Sites/infrastructure/deploy/makefile-common``

